experiment:
  seed: 42
  epochs: 10000
  lr: 0.0003

stats:
  iter_update_freq: 100

data:

  slice_len: 3.0
  train_ratio: 0.95
  batch_size: 32
  label_type: "vad"
  exclude_subjects: []

  norm:
    n_sample_batches: 8
    per_channel: True
    scaler_conf:
      standard_scaler:

  dataset_sizes:
    Armeni2022: 269
  
  preproc_config:
    armeni2022:
      filtering: True
      resample: 250
      notch_freqs: [50, 100]
      bandpass_lo: 0.5
      bandpass_hi: 125

model:
  lstm:
    feature_dim: 64
    hidden_dim: 128
    num_layers: 2
    output_classes: 2
    sub_block:
    data_block: